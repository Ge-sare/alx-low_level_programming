#include "main.h"

/**
 * mul - Multiplies two integers
 * @x : The first number to be multiplied
 * @y : The second number to be multiplied with the first number
 * Return: The result of multiplication between x and y
 */

int mul(int x, int y)

{
	return (x * y);
}
